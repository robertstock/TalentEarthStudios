import "./styles.css";
import { mount } from "./app.js";

// If the provided HTML has its own structure, this may do nothing.
// If you replace the HTML with your own, include <div id="app"></div>.
mount();
